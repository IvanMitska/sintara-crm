# Скопируйте в .env. Переопределяет значения из app.config.ts.
# APP_ENV: development | preview | production
APP_ENV=development

# Адрес backend (без /api — он добавляется в src/api/client.ts).
# Android-эмулятор использует 10.0.2.2 вместо localhost.
API_URL=http://localhost:3001
